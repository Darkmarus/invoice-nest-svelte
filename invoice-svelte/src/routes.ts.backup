import { createRouter } from 'sv-router';
import Index from './routes/index.svelte';
import Layout from './routes/layout.svelte';
import Productos from './routes/productos.svelte';
import ProductosCrear from './routes/productosCrear.svelte';

export const routes = {
  '/': Index,
  layout: Layout,
  '/productos': Productos,
  '/productosCrear': ProductosCrear,
};

export const { p, navigate, isActive, preload, route } = createRouter(routes);
